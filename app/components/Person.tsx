import Person from "../types/Person"
export default function Profile({person}): {person: Person}

(){
    return (
        <>
        <h1>{Person.name}</h1>
        <img
        className="avatar"
        src= {Person. }
        </>
    )
}